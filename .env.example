# Local secrets. Never commit this file - it is gitignored for a reason.
# Copy to .env and put your real key in it:  cp .env.example .env

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional. Real venue ratings and Google Maps links.
# https://console.cloud.google.com/ -> enable the Places API (New)
GOOGLE_MAPS_API_KEY=
